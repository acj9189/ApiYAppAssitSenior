<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AssistSeniorMovil.View.MenuInicio"
             BindingContext="{Binding Main, Source={StaticResource Key=Locator}}" 
             Title="Assist Senior">
    <ContentPage.Content>
        <ScrollView BindingContext="{Binding MenuInicio}">
            <StackLayout>
                <Button Command="{Binding consultarEnfermeroCommand}" Text="Consultar Enfermero"></Button>
                <Button Command="{Binding SolicitarServicioCommand}" Text="Solicitar Servicio"></Button>

            </StackLayout>
 
        </ScrollView>
       
    </ContentPage.Content>
</ContentPage>